[[postgres/postgreSQL|postgreSQL]]
[[PostgreSQL DB.canvas|PostgreSQL DB]]
# 🐘 Запросы PostgreSQL

---
## Категории
*   [[postgreSQL Запросы категории «оптимизация»|Запросы для оптимизации]] (`$= dv.pages('[[types/Запрос PostgreSQL]]').where(p => p['Категория запроса']?.toLowerCase().trim() == 'оптимизация').length`)