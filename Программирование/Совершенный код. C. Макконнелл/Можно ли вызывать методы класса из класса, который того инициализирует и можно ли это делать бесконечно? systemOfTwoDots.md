Глава 6.3 страница 146

Допустим у нас есть класс A, в котором есть переменная, являющаяся классом B и в нем есть переменная - класс C, суть думаю уловили.

==Какие методы можно вызывать через экземпляр класса А?==
1. Все методы класса А: a.methodA1(), a.methodA2()
2. Все методы класса B: a.b.methodB1(), a.b.methodB2()
На этом все)
==__a.b.c.methodC1() – вызывать нельзя! Это нецелесообразно__==

То есть вывод такой: __==в вызове может быть максимум 2 точки==__

[[Классы нужно связывать между собой максимально просто. Иначе вам капздец. (С примером)]]