Если атрибут функции будет изменен в теле функции, то параметр не изменится. Так как атрибут – это копия ссылки на значение.
То есть параметр  `a --> 10 <-- b` и атрибут.

```python
a = 10
some(a)
print(10)

def some(a):
	a=0
	print(a)

# 0
# 10
```