Глава 5.3 страница 92

Сокрытие информации - отличный инструмент, ускоряющий разработку ПО.

Например, вы связываетесь с пользователями через графический интерфейс. Связь эту же ==стоит прописать в 1 классе==, для того чтобы ==при переходе на консоль, вам следовало изменить лишь этот 1 класс (подсистему).==

Или, например, глобальное использование одного элемента данных. Например массив из 1000 employees. Этот пример мы уже разбирали. Если программа десятки раз обращается к этому методу напрямую, то в случае изменений требований нужно будет изменить десятки кусков программы. ==Поэтому описываем отдельный метод, который взаимодействует с этим массивом данных.==

==__Также неявную рекурсию стоит избегать__==.

[[Пример сокрытия и упрощение программы на примере ID. Главное правило функций и методов.]]