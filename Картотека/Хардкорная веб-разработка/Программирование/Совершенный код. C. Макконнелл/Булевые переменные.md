Глава 12.5 страница 292

Используйте логические переменные как можно чаще. Вместо непонятной проверки if document.isExist && not inputError && 0xe1 && lineCount < MAX_LINES, ==оберните это все в переменные, чтобы получилось:==

	if documentEsists && legalLineCount && not inputError

где
legalLineCount = (MIN_LINES < lineCount) && (lineCount < MAX_LINES)
inputError..

Согласитесь, такое читать же намного легче? Поэтому оборачивайте код в такие переменные, а не просто проверяйте на непонятные значения.

[[Типы переменных]]