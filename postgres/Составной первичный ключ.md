При составном первичным ключе ни одно из его полей не может быть `null`.

```sql
create table some_table(
    some_id int, -- автоматически not null
    some_field varchar(255), -- автоматически not null
    primary key(some_id, some_field)
);
```