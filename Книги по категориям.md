---
cssclasses:
  - wide-85
---

# IT
[[Книги категории «разработка»|разработка]](`$= dv.pages("").where(page => { if (page["Тип"] && page["Тип"].path == "types/Книга.md" && !["templates", "templater"].includes(page.file.folder) && page.Категории && page.Категории.contains("разработка")) { return page; } }).length`),

# Как жить

[[Книги категории «саморазвитие»|саморазвитие]](`$= dv.pages("").where(page => { if (page["Тип"] && page["Тип"].path == "types/Книга.md" && !["templates", "templater"].includes(page.file.folder) && page.Категории && page.Категории.contains("саморазвитие")) { return page; } }).length`),

# Художка


[[Книги категории «художественная»|художественная]](`$= dv.pages("").where(page => { if (page["Тип"] && page["Тип"].path == "types/Книга.md" && !["templates", "templater"].includes(page.file.folder) && page.Категории && page.Категории.contains("художественная")) { return page; } }).length`),