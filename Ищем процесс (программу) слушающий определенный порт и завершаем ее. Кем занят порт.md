ѳ[[Linux UNIX]]
[[Linux Unix.canvas|Linux Unix]] — канва

1. Найходим порт, слушающий порт 8000:

```bash
lsof -i :8000
```

Получаем примерно вот такой список:

```bash
COMMAND     PID              USER   FD   TYPE             DEVICE SIZE/OFF NODE NAME
com.docke 28333 ivanaleksandrovci  223u  IPv6 0x849b74b63eb7fbcd      0t0  TCP *:irdmi (LISTEN)
```

2. Завершаем процесс

```bash
kill 28333
```
